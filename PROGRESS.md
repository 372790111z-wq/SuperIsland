## 2026-09-14 — 20260914114254 native click/dismiss/reentry accepted

User confirmed “能切换”, then “图标条消失，再次悬停也正常”. The tested card activation, native Cmd-Tab strip dismissal and mouse hover after reentry pass on installed20260914114254/PID41649. Runtime records contain validated native-absence readbacks and successful exact AX/frontmost focus readbacks, consistent with that report. Acceptance is scoped to the tested interactions; it does not establish every app/window state, destructive close, or deferred primary-WeChat capture coverage.

Evidence: parent .codex-audit/wins/2026-09-14/cmdtab-commit-cleanup/native-acceptance.json and interaction-state-accepted-retest.jsonl. Current package/source evidence and the185152 rollback copies remain preserved. No source/install/permission change, commit or push occurred in this acceptance turn. Earlier pending-retest sections below are historical.

## 2026-09-14 — native commit repair 20260914114254 installed; native retest pending

Following explicit user confirmation, candidate20260914114254 was installed at /Applications/SuperIsland-WE1-Debug.app after normal WE1-only exit, then launched as PID41649. Installed129-entry manifest and strict signature match the reviewed candidate. Native Recheck reports Accessibility and Screen Recording effective; master, Dock Preview, Cmd-Tab Plus, Mission Control and its Command-W control stay enabled. Settings is closed. Main WeChat17311, Work217755 and Dock570 remained unchanged across installation.

Actual old installation185152 is preserved at /Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260914114254.app with its complete manifest verified; the separate packaged recovery copy also remains. No permissions, source code, Git HEAD/remote or unrelated app were changed. Evidence: parent .codex-audit/wins/2026-09-14/cmdtab-commit-cleanup/installation-state.json. User has been asked to test exact card activation, native strip disappearance and reentry hover; startup/settings checks are not native feature acceptance. The candidate preparation records below describe the pre-install state.

## 2026-09-14 — native Cmd-Tab commit candidate 20260914114254; not installed

User confirms hover × appears, but card activation fails and the remaining UI is the native application icon row. Current installed20260911185152 logs show three image-bearing, actionable WeChat cards and correct main-window click hits; the earlier Work2-empty AX/main-transparent capture snapshot does not explain this reproduction. Ordinary commits previously activated and removed WE1 tracking without dismissing the native strip; shared commits removed tracking before verifying dismissal. A keyboard-tap timeout was also recorded, without proof that it caused the reported gesture.

- All explicit card clicks now share a ticket-guarded native-dismissal/absence-confirmation/target-activation flow. A single marked HID Escape is sent only against validated visible native context; already-absent readback proceeds without another key event. Ordinary unselected Command release stays native. Exact owner/lifetime/window validation and existing Dock separation remain.
- Pending physical Command release cannot enqueue a duplicate commit. New input cancels the pending action, not another ticket. Failure, tap recovery and uncaptured mouse down/up trigger one deferred global native-state check: confirmed absence ends the local session, visible native UI restores pointer selection, and unknown state never activates. Ordinary keys without Command pass through. There is no background recovery polling; exceptional fresh AX discovery has a per-context one-shot 120 ms/32-node budget and can conservatively reject unusual or unavailable hierarchies.
- Final source passed260 tests (20 new coordinator/workflow/recovery tests), independent non-test Debug build and Analyze. Candidate20260914114254 is signed with the existing fixed certificate, has the same designated requirement as installed185152, all34 settings resources, no test runtime or production URL registration. Payload SHA256:5048614e078f3c3184557120f5bbf3fac321a0904a8cb6c46c93389c87605bc4.
- Current installation20260911185152 is unchanged. Its identical full129-entry recovery package is build/WE1-Debug/backups/SuperIsland-WE1-Debug-20260914-114254.app. Exact before sources, final125 Swift sources/hashes, three-file Swift delta, logs, manifests and evidence are in parent .codex-audit/wins/2026-09-14/cmdtab-commit-cleanup/. Inherited dirty changes remain; no commit, push, dependency or permission change.
- New installation requires user confirmation. Native acceptance remains open: click a true card under held Command, verify exact window and disappearance of the system strip, then re-enter and hover; repeat across both WeChat owners and an ordinary app, including release during click and a native-tile click. Automated checks do not prove HID delivery or actual desktop timing.

## 2026-09-11 — shared WeChat interaction 20260911185152 installed; native acceptance pending

User retest of installed 20260911181204 confirms both WeChat entries appear, but reports unavailable previews/close buttons/clicks. This is not native operation acceptance. In the 18:31 CST reproduction, the shared model contains two cards, one actionable/closable canonical main window, yet its frame count drops from two to one within generation 19. Only Work2 fallback PID555/WID5945 remains hittable. Main PID75262/WID5964 has exact AX ownership; Work2 AXWindows succeeds empty and focused/main/lifecycle are absent. The later bounded metadata probe confirms direct children contain only two menu bars for Work2, so no AXChildren recovery branch was added.

- Fix: Cmd-Tab frame reports carry per-NSView owner tokens. An old instance cannot erase or overwrite a replacement frame within the same generation. Current-owner detachment removes the frame, with a tombstone preventing older callbacks from resurrecting it. Existing generation checks and Dock callback behavior remain.
- Shared previews now run the existing three bounded AX refresh attempts for missing owners even while another owner's real cards are displayed. Recovery merges only canonical candidates with real AX operation objects, retains the prior exact selected identity, and cannot launch another retry cycle. Sequence, generation, PID/lifetime and native-selection guards reject stale results. Persistent empty AX remains unresolved; if both owners start empty, recovering one still ends that retry cycle.
- Two new hidden-NSPanel regressions cover single-to-shared-card reporter handoff, old positive/nil callbacks, first-click/close preservation, fallback nonactionability, and generation changes. All 240 tests, independent Debug build/Analyze, stable certificate signature, source/package checks and 34 resources pass. Bounded interaction diagnostics now distinguish image presence from hit/operation capability without storing window text or pixels.
- Following user confirmation on 2026-09-13, build20260911185152 is installed and running as PID75143. The full installed manifest and strict signature match the verified candidate. Native Recheck reports Accessibility and Screen Recording effective; master, Dock, Cmd-Tab, Mission Control and Command-W remain on, and settings is closed. Both WeChats retain PIDs75262/555; no permission reset, commit or push. Actual old installation is preserved at /Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260911185152.app with its complete manifest verified. Old package backup build/WE1-Debug/backups/SuperIsland-WE1-Debug-20260911-185152.app matches the installed full manifest. Exact before images, three-file Swift delta, test/build/package logs and probe evidence are in parent .codex-audit/wins/2026-09-11/wechat-shared-interaction/.
- Outstanding: actual native hover/close/click acceptance; Work2 persistent absent AX and both-card screenshot status; primary transparent capture remains deferred. Unit tests do not establish the exact runtime reporter that sent the observed nil or prove native commit timing.

## 2026-09-11 — shared WeChat Cmd-Tab 20260911181204 installed; native acceptance pending

The user explicitly accepted separate per-installation Dock previews and a shared all-window Cmd-Tab preview from either WeChat icon, with owner labels and no extra instance chooser. This supersedes only the earlier requirement to preserve per-icon WeChat Cmd-Tab isolation. Dock, other applications, canonical window admission, thumbnail ownership and the separately deferred primary-WeChat transparent capture issue are unchanged.

- The scoped policy recognizes only the verified primary and Work2 canonical paths plus their exact bundle IDs. Shared display is distinct from native tile identity. Complete weak-title leaf evidence may select this display group; unresolved stronger values, foreign same-name applications, unknown installations, nested AppEx bundles, and incomplete evidence do not become a shared match.
- Canonical windows retain their real owner PID/window ID/lifetime and display a primary/secondary WeChat label. Missing-owner preview-only fallback remains at most one per owner under the existing surface policy, explicitly labelled and nonactionable; it is not proof of a complete real-window inventory.
- No explicit window choice means native Command-release behavior. Shared Command-W/Q cannot default to the first card. Tile changes, close/reconciliation, nonactionable fallback hover and pending native selection invalidate old choice. Queued pointer actions preserve the real window and lifetime instead of trusting indices after a refresh.
- Shared explicit commit verifies the exact target, sends a marked Escape only to the validated Dock process when needed, and waits for successful parent-children readback showing the old switcher removed without a replacement. Unknown states fail closed. New sequence/key/click, suppression, process changes and delayed stale callbacks stop activation; mouse input is checked through the subsequent exact-focus retry. This is not an atomic guarantee against Dock input timing; real rapid-input acceptance remains required.
- Final reviewed source passed 238 tests (12 new sharing-policy tests), independent non-test Debug build and Analyze, stable deep/strict signature, package/source checks and all 34 settings resources. Candidate payload SHA256: 60365adee3de3b99728aaf19fdeb60a90cb3faf7a28ca3e8ac2f71dc33683e84. No XCTest payload or production URL scheme is packaged.
- Following explicit user confirmation, build20260911181204 was installed at 18:23 CST and is running as PID55518. The candidate/installed full manifests and deep strict signatures match. Native settings recheck reports Accessibility and Screen Recording effective; master, Dock, Cmd-Tab, Mission Control and Command-W remain enabled. Main WeChat75262 and Work2555 remain running; WINS absent. The settings window is closed. Native preview/commit acceptance is still pending user observation. Its predecessor is verified at build/WE1-Debug/backups/SuperIsland-WE1-Debug-20260911-181205.app. Pre-edit Swift/docs snapshots and exact three-file Swift delta are in parent .codex-audit/wins/2026-09-11/wechat-cmdtab-shared/. No commit, push, permission reset, dependency change or other worktree edit.
- Current WE1 feature-3 PRD records the user-approved clarification with unchecked acceptance items. Existing annotation JSON covers an earlier feature and has no WE1 mapping; no annotation migration or new viewer/HTML flow was introduced for this authorized native repair.

## 2026-09-11 — native identity diagnostics 20260911170348 installed; live samples captured

Candidate 20260911170348 adds persistent, bounded identity evidence to the existing native Cmd-Tab ambiguity path. It does not change app selection, canonical-path grouping, window actions or the user interaction. The user explicitly approved installation, completed at 17:18 CST. Running WE1 is now build 20260911170348, PID 44743. Earlier hover/MC acceptance remains historical and scoped to its recorded scenarios.

- Native selected-tile snapshots request evidence only after the full lineage remains ambiguous; mouse candidates and fallback probes do not consume the sampler. A successful narrower match never schedules ancestor ambiguity diagnostics.
- A Debug/WE1-only utility queue reads at most 20 AX fields and up to four known candidates/references. Only one worker may exist; the 0.5-second reporting deadline never frees a blocked worker slot. No AX timeout/attribute/action is changed; late results are discarded, and new starts are at least two seconds apart.
- Local `native-identity.jsonl` uses the existing 0600/1 MiB rotation recorder with separate admission counters. Every sample has at most eleven records of fixed state codes, numbers and flags. Known app URLs are compared only in memory, never persisted. `completed` means the sampling flow ended, not that a correct native tile-to-app binding was established.
- 226 tests passed (six new policy/budget/privacy tests), independent non-test build and Analyze passed, stable signature and 34 settings resources verified. Candidate payload SHA256: `6b3d931b5bdfae150f29435952a615b6a64204974ff40d3edc0d7f30d9a35c78`.
- The installed candidate matches the full verified package manifest. The old 20260911102000 installation was preserved with its full manifest at `/Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260911170348.app`. A verified ZIP is saved under parent `.codex-audit/wins/2026-09-11/persistent-native-identity/before/`; the previous packaged candidate is preserved at `build/WE1-Debug/backups/SuperIsland-WE1-Debug-20260911-170349.app`. Only WindowCommandTabMonitor.swift plus the new helper/test differ among Swift inputs from the pre-edit snapshot. Inherited changes, Git HEAD and remote state are preserved.
- Installation/startup and settings recheck are complete: Accessibility and Screen Recording are effective; master/Dock/Cmd-Tab/Mission Control/Command-W remain enabled. Main WeChat PID 75262 and Work2 PID 555 were preserved, and WINS remains absent. Native identity logs were subsequently captured during the user retest; see the live-evidence paragraph below. Two-WeChat mapping remains unresolved. Evidence/review: parent `.codex-audit/wins/2026-09-11/persistent-native-identity/` and `/Users/muyz/.codex/reviews/20260911-161748-952-WE1-persistent-native-identity-diagnostics.md`.

Live user retest at 17:21:03/17:21:15 CST produced two complete samples (14 records, no parse errors, duplicate events, missing required records, timeout or truncation). Both sampled Dock-owned buttons match the two known main-process candidates, but AXLinkedUIElements and AXServesAsTitleForUIElements return unsupported (-25205), and AXTitleUIElement returns no value (-25212). No usable reference was returned. The user reports visiting both WeChat icons; the current log has no per-tile unique identifier and does not independently prove separate coverage. This evidence closes the repeated three-attribute sampling attempt for this state, not every possible identity route. Dual-WeChat mapping remains unresolved. See native-identity-user-retest-summary.json and the immutable selected-record copy.

## 2026-09-11 — preview controls 20260911102000 installed; native acceptance pending

User authorized continuing after reporting missing dual-WeChat Cmd-Tab previews, missing Cmd-Tab hover close, and missing Mission Control hover/shortcut close. This independent `fix/we1-preview-controls-20260911` worktree starts from HEAD 406f47c plus the complete, hash-verified source of installed 20260911021508 (including preview performance and Zilan compatibility). The original source worktrees remain unchanged. Following explicit user confirmation, candidate 20260911102000 replaced the installed application at 10:30 CST.

- Mission Control now recovers an unobserved session using a motion-driven, throttled, direct-Dock-root probe, then runs the existing exact-window resolver. Empty/slow/failed probes remain nonactionable; old callbacks cannot restore a stopped, changed-Space, or suppressed session. No permanent new polling loop or relaxed window ownership checks.
- Cmd-Tab frame generations repair a concrete retained-view hit-region gap after membership/loading changes. Ordinary cache-to-fresh publication keeps the same frame generation and pending first-click behavior. This is not proof that every reported missing close button has the same cause.
- Debug-only local interaction metadata distinguishes pointer ingress, card frames/hits, close capability, native tile matching and Mission Control recovery. No titles, URLs, paths or pixels; 16 pending writes, 12 admissions per rolling second, per-event 500 ms, current+previous 1 MiB files.
- Final 220 tests passed, non-test Debug build and Analyze passed, candidate packaged and stable signature verified. All 34 settings assets match source; no XCTest bundle or production URL scheme is packaged. Source/record evidence: parent workspace `.codex-audit/wins/2026-09-11/preview-controls/`.
- Candidate: `build/WE1-Debug/SuperIsland-WE1-Debug.app`, build 20260911102000, payload `acfe3e677c88c65cece5629e14b459e4e7fb73446fc766ad9e2f773291f89231`. Installation is complete; real hover/close/shortcut acceptance remains pending. The existing source warnings in AutoUpdater, WindowThumbnailProvider and VolumeManager are outside this change.
- Installation verified: the complete staged/installed candidate manifests match; the old app was normally quit and preserved at `/Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260911102000.app`, matching the existing checkpoint manifest. New WE1 PID 86852 reports build 20260911102000; primary WeChat PID 75262 and Work2 PID 555 remain running, and WINS remains absent. Native settings recheck shows Accessibility/Screen Recording effective and Dock/Cmd-Tab/Mission Control/Command-W enabled. The new local `interaction-state.jsonl` is writing build-specific metadata with mode 0600. These startup checks are not native behavior acceptance. See `installation-state.json` in the evidence directory.
- Dual-WeChat native tile identity remains unresolved. `source=commandTab` window inventory also records activation prewarm; it does not prove native selection resolution. Linked AX associations remain an unobserved evidence path, not a implemented identity fix. Existing one-step interaction and cross-installation separation remain required.

- Follow-up on DoubaoWork and Tencent Lemon: affected Cmd-Tab samples had exact pointer hits but empty AX windows/focused/main/lifecycle sources, so the previews correctly lacked operation objects. The same PID/window IDs later returned AX close buttons. User confirmed both now show hover × on reentry; 02:43:10/11Z pointer logs agree (`hitCanClose=true`, one activatable/closable candidate). First-entry empty-AX trigger and actual close action remain unverified. No additional business-code or app-install changes were made. Narrow evidence: parent `.codex-audit/wins/2026-09-11/doubao-lemon-controls/`.

- Mission Control user retest: after instructions to hover a disposable Finder window and press Command-W, the user replied “出现，使得。” This is recorded as hover × appearance and target-close success for that tested window. Process readback still shows WE1 86852 and no WINS. Current metadata does not independently prove the close action, and reentry/Space/display coverage remains open. Dual-WeChat Cmd-Tab identity diagnosis continues separately. See `native-acceptance.json`.

## 2026-09-10 当前：性能优化前的本地基线

用户已确认先保存 Git，再建立 `perf/we1-preview-latency` 分支和独立 worktree。原目录保留在 `fix/we1-window-inventory-gate-20260907`。本次仅保存现有实现、测试、需求与原型资料，并整理当前状态；性能代码尚未修改。

6 个 Swift 文件与 build **20260910120616** 的最终源码清单逐项一致。已有成功测试证据为 `zcode-closed-preview/logs/reviewed-tests.log`（155/155，0 失败）；本次未重跑测试。主微信图片预览、双微信 Cmd-Tab 无卡仍未解决。增加实例选择步骤的候选方案未获接受，不作为当前实施方案。

WINS 对比和后续优化方向、验收边界与恢复输入见 [性能优化前基线说明](docs/checkpoints/2026-09-10-preview-performance-baseline.md)。以下保留历史记录；其中旧 PID、current/latest、未提交状态及 PRD 原始分支不代表本次保存后的当前状态。

## 2026-09-10 latest: Cmd-Tab WeChat no-card cause confirmed

The user reports both WeChat entries completely lack cards. A bounded live AX probe and the actual WE1 PID45493 CmdTabPlus stream agree: native WeChat AXButton exposes name-only identity matching two main installations plus their two AppEx processes; distinct bundle paths cannot form a valid identity group, so native-sync retries hide the overlay before capture. This is separate from the deferred primary Dock pixel issue. Background prewarm shares source=commandTab, so earlier inventory alone was not presentation evidence. Other apps with missing previews remain unidentified.

No production repair was made. The candidate explicit-instance chooser for ambiguous native CmdTab selections is awaiting user decision; no implicit cross-installation grouping or guessed tile/PID mapping is authorized. Optional full-attribute/link sampling did not overlap a visible switcher, so availability of additional system links remains unknown. All bounded probes/stream ended; installed WE1 and both WeChats are preserved. Evidence: parent workspace `.codex-audit/wins/2026-09-10/cmd-tab-preview/diagnosis.json` and `diagnosis.md`. Earlier entries below are historical.

## 2026-09-10 acceptance direction update

The user said “暂时先这样，看别的吧。” Primary WeChat main-window capture is deferred, still unresolved and not accepted. Continue with Cmd-Tab Plus native preview and exact-window selection; first check Chrome thumbnail display and Up/Down selection followed by Command release. No close/quit action is part of this first check. WE1 PID45493 remains active, WINS is absent, and current-session commandTab metadata exists; user-visible behavior is awaiting user observation. Existing accepted behavior and source changes remain preserved.

## 2026-09-10 latest: WINS comparison completed, WE1 restored

- User-supplied WINS screenshots reproduce the primary WeChat main-window blank thumbnail; primary image/article windows and secondary WeChat main/article windows have real thumbnails. This failure is not unique to WE1. Underlying cause is still unknown; no signature, privacy-setting or OS root cause is established.
- WINS PID43537 normally terminated via exact-path-validated NSRunningApplication.terminate(); absence verified. Restored installed WE1 build20260910120616 as PID45493, session561592D6-D5E6-4CFE-AC38-EC3C030D77CC. Native master, Dock Preview and Cmd-Tab Plus are on; both permissions effective after Recheck. Settings closed. Main WeChat40956 and Work2555 were preserved.
- Original WE1 PID38234 was absent on return, cause unknown. It was relaunched, so its memory-only retirement ledger did not survive the control. Earlier ZCode acceptance remains scoped historical user evidence, not a new post-restoration acceptance. Single-display Dock pin is currently unfixed after the pause released its previously paused/nonactive request.
- Primary WeChat capture remains unresolved. No business code, app installation, signing, TCC, WeChat data/settings, commit or remote changes. Evidence: parent workspace `.codex-audit/wins/2026-09-10/wechat-main-window-capture/wins-control-state.json` and `diagnosis.md`. Following entries preserve earlier checkpoints and their process IDs.

## 2026-09-10 ZCode closed-preview repair installed (current)

- Candidate **20260910120616** prevents explicitly retired windows from reentering weak discovery in Dock and Cmd-Tab. The three-file change adds process-lifetime/window-ID exclusions, valid exact AX reestablishment, retirement-aware renderer selection and independent pixel-owner capture epochs. Existing dirty work and Chrome grouping are preserved.
- Final verification: **155/155 tests passed** (11 new), independent non-test build and Analyze passed, deep/strict signing passed, and independent source re-review has no remaining blocker. Source payload SHA256: `5cdadce0c8a650498e49a9406d954f07399298cc328cf06cf9cb1b1723199faa`.
- User confirmed the default installation and normal ZCode restart option. Build **20260910120616** is installed at `/Applications/SuperIsland-WE1-Debug.app`, running as PID38234. Complete candidate/staged/installed file, symlink and mode manifests match; the old installed manifest matches the recoverable backup `/Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260910120616.app` (20260910105458). Deep/strict signature verification and UI permission recheck passed. No permission, certificate, source or Git changes during installation.
- Old WE1 PID20869 and ZCode PID551 were normally quit. New WE1 was running before ZCode restarted as PID38300. Session `EEA8C30F-07C0-4B85-A89D-F0963CAF4C2F` exact-bound new ZCode WID3018 at 2026-09-10T08:12:15Z. The user subsequently confirmed "zcode 好了" in response to the Dock preview close -> re-hover check. This reported ZCode issue passes user acceptance; broader window states are not implied. The accompanying screenshot and feedback confirm the main WeChat issue remains. CUA has no native hover action. WINS is absent. Old development SuperIsland PID1085 was preserved; no definite preview interference was established from its metadata.
- WeChat main-window activation succeeds but preview does not recover. Main1655 is marked SharingNone; Work2 main886 is SharingReadOnly with the same application version. Independent exact/renderer capture and safe fullSize comparisons have not restored main content. The cause of the sharing-state difference is unknown. The user subsequently authorized only a normal primary-WeChat restart: main534 exited and main40956/new mainWID3115 was opened; the user completed login. Work2 remains555/886. The user confirms primary preview still unavailable, corroborated by raw all-zero screenshots and a bounded exact-window stream comparison; trusted renderer3119 also remains uniform. No privacy override was attempted. No sharing-state override or capture-flag change was made. Original startup-transition and Cmd-Tab acceptance remain open.
- Installation and live acceptance: parent workspace `.codex-audit/wins/2026-09-10/zcode-closed-preview/installation-state-20260910120616.json`, `native-acceptance.json`, and `installed-inventory-observations.json`. Installation review: `/Users/muyz/.codex/reviews/20260910-160438-427-WE1-20260910120616-confirmed-installation-and-ZCode-restart.md`.
- Evidence, final source snapshots, incremental diff and restoration: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-10/zcode-closed-preview/validation.md`.

## 2026-09-10 WeChat repair installed (previous installed baseline)

- User confirmed installation of build 20260910105458. Installed and launched from `/Applications/SuperIsland-WE1-Debug.app`, PID20869. Full candidate/installed file and symlink manifest, backup manifest, and deep/strict signature all match. Both permissions remained effective after UI Recheck; Dock preview and Cmd-Tab Plus remained on.
- Previous actual installation 20260909175743 is preserved at `/Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260910105458.app`. WINS is stopped; WeChat PID534 and Work2 PID555 were not restarted or terminated. No source changes, commit or push during installation.
- Current native preview acceptance failed: the user reports two actual WeChat windows and two cards, with the main-window card unavailable and the article card working. Runtime inventory resolves real main WID1655 and article WID2208 for PID534; old WID259 is neither retained nor resolved. Independent metadata-only capture sampling found transparent main-window samples and uniform related-renderer samples, while article captures contained normal variation. This is corroborating probe evidence, not WE1 internal capture logs. Main-window activation/re-hover comparison is pending; diagnosis continues in the adjacent `wechat-main-window-capture` audit directory.
- Original startup retirement still requires candidate continuously running while a temporary window becomes exact-bound and then retires. Current post-restart counts cannot establish that transition. Cmd-Tab acceptance remains paused until the WeChat preview failures are checked.
- Installation and current acceptance evidence: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-10/wechat-retired-window/native-acceptance.json`.

## 2026-09-10 WeChat retired-window repair candidate (pre-install checkpoint)

- New regression on installed build 20260909175743: startup WID259 (main WeChat) and WID142 (Work2) survived only as private surfaces and historical bindings, producing a blank card. Prior accepted ordinary-window scenarios remain scoped historical passes; startup retirement was not covered.
- User authorized the repair after diagnosis. Five files changed this turn: lifecycle/capture provider, inventory history, both consumers, and inventory tests. Exact AX retirement and invalidUIElement cleanup revoke bindings; observer revisions reject stale work/notifications; bounded per-lifetime cache epochs isolate retirement from unrelated apps. Existing Chrome shared-preview changes and its 48 interaction tests are preserved.
- Final reviewed source: 144/144 tests passed, independent non-test Debug build and Analyze passed, two independent source reviews completed. Candidate 20260910105458 is signed and deep/strict verified; source-payload hash 4af76248166641c840bc7ae4cc084956c4f37f01566333b5bfa6375d60053fef. No XCTest linkage/payload.
- Installed build remains 20260909175743. No installation, app quit/restart, commit, or push occurred in this repair turn. Original WeChat startup-transition acceptance is pending; merely restarting WE1 clears history and is insufficient proof. WINS was running after reboot and must be excluded for clean native acceptance. Cmd-Tab acceptance is paused until this regression is checked.
- Evidence and scoped restoration: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-10/wechat-retired-window/validation.md`; per-turn baseline and incremental diff are alongside it. Existing accepted checkpoints and untracked resources are preserved.

# SuperIsland Window Enhancement Goal Progress

Updated: 2026-09-09

## 2026-09-09 Chrome shared preview installed (current)

- Accepted local snapshot: `/Users/muyz/Projects/new super island/.codex-audit/checkpoints/we1-20260909175743-chrome-accepted/`. Actual installed-app ZIP was extracted, file/symlink compared and signature verified. It also contains a complete 217-file tracked source baseline verified against Git blob IDs, the 2 reviewed file overlays, all 34 required untracked build resources, diff, manifests, acceptance records and restoration README. This is a local snapshot, not a new Git commit or push; it excludes user data, permissions and signing credentials.
- User explicitly confirmed installing the reviewed shared-preview candidate. `/Applications/SuperIsland-WE1-Debug.app` now contains build **20260909175743**, launched from that exact path as PID **97991**. Installation startup and signature/source verification passed. The user subsequently confirmed both Chrome Dock icons show the same complete list (reply: “可以，继续。”); popup/list acceptance passes. The user then replied “可以” to the separate single-click check for a card from each Chrome instance: both tested activations pass. The user then replied “确认” to the disposable blank-window close check: only the chosen window closes, other windows remain, and its card disappears on re-hover. That tested close scenario passes; the specific Chrome instance was not identified. The user then confirmed both WeChat Dock icons retain separate ownership, complete/unique lists and correct card activation (“是的，没问题”). This Chrome repair and the specified WeChat regression pass for those tested scenarios.
- Candidate and same-volume staging copy were compared file-by-file and deep/strict signature checked before replacement. Old WE1 quit successfully via its visible Exit control after the AX action did not respond; no force kill was needed. All target-path processes were confirmed absent before replacement. The installed app matches the candidate manifest, and the old backup matches the original installed manifest.
- Rollback: old accepted build **20260909151823** is preserved intact at `/Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260909175743.app`. The earlier accepted archive/source/resources checkpoint remains untouched. The unique Applications staging directory was removed only after the staged app was moved successfully; no obsolete app copy remains in Applications.
- WINS was observed running as PID79649 before installation. Following the user's prior explicit quit request for this same acceptance task, attempted native control (timed out), then revalidated its exact executable and sent TERM. It stopped; no WINS process was present before swap or in startup readback. No WINS settings/data/launch items were changed.
- Newly launched WE1 Settings reports Accessibility and Screen Recording effective and Dock Preview enabled. Recheck leaves those effective statuses unchanged. No TCC grants, certificate trust or feature preferences were changed.
- New-build Chrome commandTab diagnostic samples resolve one real window without rejection; they do not prove a Dock popup or card action. The user has now confirmed both Chrome icons show the same complete list. New-build Dock diagnostics also enumerate PID560 and PID28655 with one real window each and no rejection in the observed samples. The user subsequently confirmed one single-click card activation from each Chrome instance. The user confirmed the disposable blank-window close scenario. The user has now also confirmed both WeChat Dock icons retain separate ownership, complete/unique lists and correct single-click activation on this Chrome repair build. After the accepted snapshot, the user replied “可以 i” to the minimized-window check: one minimized Chrome window remained in the Dock preview and its card restored/activated the correct window. This tested scenario passes; process/instance was not specified. The user then confirmed the fullscreen check (“可以，没问题”): one Chrome fullscreen window remained previewable from the regular desktop and its card returned to the correct fullscreen window. This scenario passes without implying all multi-Space/display cases. Next validation is Cmd-Tab Plus window preview/selection. A read-only code review verified Command+Tab selection triggers the extra window panel near the native icon strip; the user has been asked to keep Command held, click one Chrome thumbnail and verify the exact window. Native app icons alone do not prove Plus. Existing PRD line145 describes a different custom icon-strip layout; no PRD/source change was made during acceptance. CUA does not expose a hover-only native action, so no synthetic click/drag or browser manipulation was substituted for that acceptance step.
- Installation evidence: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/chrome-shared-preview/installation-state-20260909175743.json`; narrow Chrome diagnostics in `installed-chrome-diagnostics.json`. Review: `/Users/muyz/.codex/reviews/20260909-180302-718-WE1-20260909175743-confirmed-installation-and-Chrome-shared-.md`.
- This installation changed no business source or Git state. Current uncommitted two-file Chrome patch is unchanged from the 130-test candidate. No commit or push. Chrome shared-list, tested card activations in both instances, and one disposable blank-window close/card-removal scenario have passed by user confirmation. This-build WeChat ownership/list/activation regression also passed. Close coverage for every instance/state, WeChat close and other window/system states are not implied. Current native acceptance evidence: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/chrome-shared-preview/native-acceptance.json`.

## 2026-09-09 Chrome shared preview candidate (pre-install checkpoint)

- The user explicitly superseded the per-icon Chrome instance requirement with “那就同样都显示就行了”. Both Dock icons of the same installation now resolve the same full process group. Different canonical application paths remain separate, even with matching names/bundle IDs. This follows the existing app-level multi-window PRD and keeps its layout/flow.
- Implementation is limited to WindowDockInteractionMonitor.swift plus 12 interaction regression tests. Hover reuse validates the actual Dock AX tile, sorted member PIDs and process lifetimes. Each member is enumerated/captured separately; publication is atomic and cancelled on changed membership. Each row preserves owner/lifetime and exact AX/window action checks. Negative fallback IDs cannot collide across members. Unique-only Dock reverse selection remains unchanged.
- Final automated verification: 130/130 Window Enhancement tests passed (82 inventory + 48 interaction), no failures/skips. Independent non-test Debug build and Analyze passed. Initial focused run compiled but two new expectations conflicted with the canonical-input/whole-group-rejection contract; tests were corrected without loosening production policy. Independent source review found no blocking issue.
- Signed candidate build **20260909175743** is at `build/WE1-Debug/SuperIsland-WE1-Debug.app`. Existing certificate identity was reused; deep/strict signature and source-payload metadata verification passed, with no XCTest payload/linkage and no production URL scheme. Source payload SHA-256: `822c46f2d077ee2ea2b186f4ccda915fcebda09193ddd4418142c05579eeccef`.
- Live read-only policy probe at 2026-09-09T09:58:00Z selected Chrome [560,28655], WeChat [3153], Work2 [4824]; Chrome reverse remained nil. This proves current roster grouping, not UI behavior. The probe did not read browser content or command-line values or activate/stop any app.
- Installed app remains the accepted **20260909151823**, PID5943. Candidate installation and both Chrome icon/card/action walkthrough are pending. Latest metadata reports WINS running again; its startup was not caused by this probe. Recheck and remove that preview-source confound before native WE1 acceptance. Previous WINS-stopped acceptance remains historical evidence.
- All new evidence, baseline snapshots, source snapshots, diff, logs, policy probe and candidate verification: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/chrome-shared-preview/`. Review: `/Users/muyz/.codex/reviews/20260909-174251-241-WE1-same-path-Chrome-shared-Dock-preview-repair.md`.
- No install, commit or push for this candidate. HEAD remains `1f37eaa416f6c22476d35bfe47da0280d1ce2175`; accepted checkpoint and untracked resources/materials are preserved. Prior candidate package moved by the packaging script to `build/WE1-Debug/backups/SuperIsland-WE1-Debug-20260909-175744.app`. To undo this source patch, restore only the two files from the new audit baseline after checking for later edits; never reset the whole worktree.

## 2026-09-09 accepted Ego Lite checkpoint (previous installed baseline)

- Current branch: `fix/we1-window-inventory-gate-20260907`, local checkpoint `1f37eaa416f6c22476d35bfe47da0280d1ce2175`. User requested saving the accepted version; the six WE1 source/test files were committed with exact-path staging. No remote mutation or push. The 2026-08-31 entries below are historical.
- Corrected the previous diagnosis: `CGWindowListCreateDescriptionFromArray` works in the current GUI session. Passing NSNumber objects instead of raw pointer-sized CGWindowID values caused empty descriptions. WINS research and same-ID-set probes are archived at `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/research.md`.
- Added `WindowServerWindowDescriptions.copy` in the existing inventory compilation unit, and routed all six inventory/Mission Control description calls through it. It uses nil CFArray callbacks and preserves nil versus successful empty/partial results. The Mission Control comment attributing this failure to transformed windows was corrected.
- Window admission, owner/PID/level/Space checks, CGS NSNumber arrays, cache completeness, retained IDs, preview-only selection and activation/close behavior are unchanged by this patch. This does not implement WINS-style startup enumeration or Space manipulation.
- Added five regression tests, including a real CoreGraphics lookup of a test-owned window that is never shown. **118 tests passed, zero failures**, including 82 inventory tests and 36 interaction tests. Independent non-test Debug build and Analyze passed. Existing warnings remain outside the three changed source/test files; no warning was reported in those files.
- Candidate **20260909151823** (arm64) is at `build/WE1-Debug/SuperIsland-WE1-Debug.app`, bundle ID `com.workview.SuperIsland.WE1Debug`. Final deep/strict signature verification passed outside the sandbox using the existing certificate-based requirement. No trust, TCC or certificate changes were made. Source payload SHA-256: `9b8a2012081d66e07c51a318c199deb346ebb80b67d413152f41559dc1768b2e`; packaged metadata matches the non-test source and no test runtime payload is present.
- **Installed and launched after explicit user confirmation.** `/Applications/SuperIsland-WE1-Debug.app` is now build `20260909151823`, PID `5943`. The exact installed package matches the candidate Info.plist/executable/debug dylib and passes deep/strict signature verification. The settings UI reports Accessibility and Screen Recording effective; no grants changed. WINS was later quit at the user's explicit request and verified stopped before acceptance. Installation/startup and the user-confirmed Ego Lite Dock scenario have passed; other product acceptance remains pending.
- Previous project candidate preserved at `build/WE1-Debug/backups/SuperIsland-WE1-Debug-20260909-151823.app` (build `20260908163754`). Pre-edit snapshot, repair-only diff, source hashes, test/build/package logs and verification are preserved at `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/cfarray-repair/`. Do not reset the entire dirty worktree to roll back this small patch.
- Review record: `/Users/muyz/.codex/reviews/20260909-150955-791-WE1-CGWindowID-CFArray-representation-repair-and-candidate-b.md`.
- Installation evidence: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/cfarray-repair/installation-20260909151823.json`. The previous actual installation is preserved at `/Users/muyz/.Trash/SuperIsland-WE1-Debug-pre-20260909151823.app` (build `20260908163754`). UI quit and TERM were ignored; only the rechecked old WE1 PID `23104` was killed, then all target-path processes were confirmed stopped before replacement. Installation review: `/Users/muyz/.codex/reviews/20260909-153207-456-WE1-candidate-20260909151823-installation-and-startup-verifi.md`.
- New-build diagnostic samples contain nonempty SkyLight surfaces and successful resolved windows (Codex 3 surfaces/1 resolved, Chrome 5 surfaces/1 resolved, with no rejection in those samples). These are diagnostic observations for those queries, not proof of Ego Lite or all-window acceptance.
- Latest new-build Ego Lite Dock log at 2026-09-09T07:39:30Z has 2 surfaces, 2 resolved entries and no rejections. The user subsequently confirmed this Ego Lite Dock preview scenario works after WINS was quit; do not extend that confirmation to unrelated scenarios.
- Accepted checkpoint: `/Users/muyz/Projects/new super island/.codex-audit/checkpoints/we1-20260909151823-ego-accepted/`. It contains the actual installed-app ZIP, 6 source snapshots, 34 untracked required build-resource files, SHA-256 manifest, and these progress records. ZIP extraction was compared file-by-file and deep/strict signature verified. The resource directories are not in Git; restore the saved resources as documented before attempting a clean-checkout build. Other untracked materials and default.profraw were not committed or deleted.
- User-confirmed multi-window acceptance: on 2026-09-09 the user reported “微信，2、2、没有” in response to actual-window count, Dock-card count, and blank/duplicate feedback. This WeChat scenario passes with 2 actual windows, 2 preview cards, and no blank or duplicate cards. The specific WeChat installation was not identified, so this does not establish both instances or cross-instance identity. Earlier diagnostic counts are separate samples, not a contradiction of this observed result.
- User-confirmed two-WeChat ownership: on 2026-09-09 the user reported “是的， 没有串，也没有漏窗或者重复” after checking the two Dock icons separately. Both instances show their own windows without cross-instance mixing, omissions or duplicates. The preceding read-only process check identified `/Applications/WeChat.app` (main PID 3153) and `/Applications/WeChat-Work2.app` (main PID 4824), with WINS stopped and WE1 PID 5943 running. This feedback verifies ownership/list completeness, not click activation or close actions.
- User-confirmed first-click activation: on 2026-09-09 the user answered “可以” when asked whether a single click on a WeChat preview card switches to its corresponding window. This observed WeChat activation scenario passes; it does not establish close-button behavior or Cmd-Tab activation.
- Chrome diagnosis before the shared-preview decision: the user reported Google Chrome cannot preview; close-button acceptance is paused. A read-only live probe on 2026-09-09T09:05:25Z confirms two regular Chrome main applications (PID 560 and 28655) share `/Applications/Google Chrome.app` and `com.google.Chrome`. Running the unmodified current Dock identity policy on these candidates deterministically returns nil, before inventory/capture. Dock exposes two application tiles with the same name and URL and no PID/identifier in the inspected attributes. The retained log has 16 Chrome records, all commandTab, with one resolved real window for each PID; this is not proof that Command-Tab visually works. The user subsequently confirmed the Dock preview does not pop up, consistent with this reproduced pre-inventory rejection.
- Chrome diagnosis evidence: `/Users/muyz/Projects/new super island/.codex-audit/wins/2026-09-09/chrome-preview/`. The second process has a custom user-data directory, remote debugging and no-startup-window flags; their values and browser contents were not collected. No process was stopped or application activated. WINS uses first exact-URL match in its inspected Dock path; this is not a verified solution for per-instance ownership. The user initially required separate previews for each Chrome Dock icon. The later shared-preview decision above supersedes that constraint; first-match/index guessing is still not used. Further AX/ActualPid/remote-token/CoreDock investigation has not produced a verified per-tile process mapping. Activation timing alone was rejected as insufficient provenance. No business change had been made at that diagnosis snapshot. The shared-preview candidate above now implements the later decision; native acceptance remains pending, with the accepted build and passed WeChat/Ego cases preserved.
- Deferred acceptance: preview close-button targeting/card removal, Cmd-Tab/MC and remaining system-state checks. The historical Docker check remains deferred because its current relevance has not been verified. The saved Ego Lite checkpoint remains the earlier snapshot; subsequent acceptance and this Chrome blocker are recorded here.

## 2026-08-31 evening preview-fix installation checkpoint (historical)

- User explicitly authorized installation. Packaged the reviewed non-test build from `/private/tmp/SuperIslandPreviewFixNonTestDerivedData/Build/Products/Debug/SuperIsland.app`; the six repaired source-file hashes still match the 70-test/build/Analyze checkpoint. No functional source, HTML or PRD changes were made during installation.
- Installed and actually launched `/Applications/SuperIsland-WE1-Debug.app`, build **20260831224636**, PID **20830**. Package, temporary installation candidate and installed app passed deep/strict signature checks with the existing `5BA2336137A6FF57316E24449C3914B56E3AC4C9` identity and certificate-based designated requirement. No certificate trust, TCC grant or hardened-runtime setting was changed.
- Source payload SHA-256: `a3181f130530812e94752b30ab77b36f513875e99522de110d48f5399fad2c4c`. Installed/package signed debug dylib SHA-256 both `35d64e4290bab3ca0ca90c2fbba86490a89013096cb96c360d9a8be5765b50d7`. No XCTest payload/linkage found.
- The new Settings window opened successfully. Both Accessibility and Screen Recording showed effective and remained effective after clicking Recheck. Existing Dock preview and Cmd-Tab Plus switches remain enabled. The old permission blocker is not current.
- Process remained alive at the 6m24s check. A 15-second `top` sample with settings open and permissions effective showed 0.2% CPU in all three measured intervals, about 64 MB in top's MEM metric and 10 threads. Separate final `ps` RSS was 156256 KiB. These are distinct memory metrics and a short baseline, not long-run or active-hover latency acceptance.
- Normally quit the old app through its Exit button before exact-path replacement. The previous installation and package (both build 20260831094911) were moved to `/Users/muyz/.Trash/SuperIsland-WE1-Debug-old-installed-20260831094911-pNdoh0.app` and `/Users/muyz/.Trash/SuperIsland-WE1-Debug-old-package-20260831094911-pNdoh0.app`. The empty installation staging directory was removed. No dated old app was left in Applications; production SuperIsland and WINS were not touched.
- Installation review: `/Users/muyz/.codex/reviews/20260831-223626-343-WE1预览修复版签名安装与启动核验.md`. Real Dock/Cmd-Tab hover, close, first-click switching, WeChat content and Mission Control acceptance still require user interaction; installation alone does not complete the WINS-replication goal.

## 2026-08-31 preview repair and adversarial review checkpoint (pre-install history)

- Scope: repair the confirmed native implementation only. HTML, PRD, settings structure, signing/TCC, WINS and production installation were not changed. Current branch remains `feature/window-enhancement-settings`; pre-existing dirty changes were preserved.
- The preceding live diagnosis of installed WE1 build `20260831094911` showed Accessibility and Screen Recording effective and feature switches enabled. The latest user screenshots still fail functional acceptance. The old permission blocker below is historical, not a reason to request authorization again.
- Dock and Cmd-Tab now use the same event-driven pointer state machine. A nonactivating AppKit hit view accepts the first click; hover drives the close affordance. Activation/close require a matching press/release on the same target and action. Geometry, capability, identity and session changes invalidate stale input.
- The Cmd-Tab pointer tap consumes only gestures begun inside the visible preview. AX actions are deferred outside the event callback. Repeated moves over the same card do not queue callbacks; native-tile AX lookup retains the existing coalescing. A held-click release drain is removed on release/stop; no new idle polling was added.
- Thumbnail capture excludes compositor shadows and samples the interior. A close dot or shadow alone is no longer content; independent AX document/window-control evidence preserves legitimate blank documents. A proven empty capture evicts its old cache entry under the lifecycle generation guard. Renderer ownership/action identity remains strict.
- Cmd-Tab has an explicit loading state and uses the existing bounded preview-only discovery when AX provides no actionable windows. These discovered cards cannot activate or close an unverified target. This is an implementation change, not proof that every WeChat process topology is now supported.
- Adversarial rechecks fixed stale-cache resurrection, capability loss during a press, transient layout changes, hover-triggered scrolling, delayed callbacks crossing sessions, old accessibility button targets, sparse AX proxies losing close capability, and long-press release leakage. No unrelated Mission Control or split-layout code was changed.
- XCTest host startup/termination/activation is isolated from business services. Final test run: **70 passed, 0 failed, 0 skipped** (44 existing + 26 new). It includes synthetic pixel fixtures, actual AppKit coordinate/event delivery in a hidden test-owned panel, and both production preview models. Result: `/private/tmp/SuperIslandPreviewFixDerivedData/Logs/Test/Test-SuperIslandWindowEnhancementTests-2026.08.31_20-12-07-+0800.xcresult`.
- Independent non-test Debug build and Xcode Analyze succeeded. Artifact: `/private/tmp/SuperIslandPreviewFixNonTestDerivedData/Build/Products/Debug/SuperIsland.app`; no XCTest payload or linkage found. Logs: `/private/tmp/superisland-preview-fix.03QbI8/tests-accepted-source.log` and `build-analyze-final.log`. Existing unrelated concurrency/CF-pointer warnings remain in the clean build; no new warning was reported in the changed preview files.
- Build is not package/install/live acceptance. Installed WE1 remains `20260831094911`, last read-only process check PID `314`; no new app was launched. Test-host/helper path checks found no leftover process after testing. Native Dock event delivery, WeChat content, first-click activation, actual close, multi-Space behavior and permissioned resource/latency acceptance still require a separately authorized installed build and real desktop checks.
- Review record: `/Users/muyz/.codex/reviews/20260831-184359-435-SuperIsland预览窗口准入与鼠标交互修复.md`. Per-turn source backups: `/private/tmp/superisland-preview-fix.03QbI8/`; do not reset the whole dirty worktree to roll back this repair.

## 2026-08-31 morning signed installation checkpoint (historical)

- The existing identity is now valid: `5BA2336137A6FF57316E24449C3914B56E3AC4C9`. Current-user trust contains exactly one WE1 policy, `Code Signing`; the admin trust query contains no WE1 entry. Certificate extensions remain `CA:FALSE`, `Digital Signature`, `Code Signing`.
- Fixed the packager's designated-requirement parser to accept both `# designated =>` and `designated =>`, and reject a code-hash-only requirement when a certificate is requested.
- The first certificate package (`20260831090758`) passed signature verification but failed real launch: dyld rejected the Debug dylib because the packager had newly enabled Hardened Runtime with a self-signed identity lacking an Apple Team ID. Removed that packaging-only runtime flag to preserve the existing non-hardened Debug mode; no exception entitlement, production setting or OS security setting was added.
- Installed and actually launched corrected build `20260831094911` at `/Applications/SuperIsland-WE1-Debug.app`, PID `62989`. Both package and installation pass deep strict codesign verification. Requirement: `identifier "com.workview.SuperIsland.WE1Debug" and certificate root = H"5ba2336137a6ff57316e24449c3914b56e3ac4c9"`.
- Source payload SHA-256: `7a1fce5ee6e789c9314d7b9089c484bb71207dcb241ed5d9525d74509f908b02`. The installation source is the separate non-test build; it contains no XCTest payloads. Latest code regression remains 44 passed / 0 failed on 2026-08-30; this turn changed only packaging and progress records.
- App UI reports both Accessibility and Screen Recording as revoked and shows the exact authorization path above. The user has been asked to grant these permissions; no TCC grant was changed by the agent. Dock/Cmd-Tab/Mission Control acceptance remains pending.
- A 45-second sample with settings open and permissions unavailable showed 1.9–2.2% CPU after the initial top sample and about 74 MB memory. This is not full-feature resource acceptance.
- Stopped three verified orphan helpers from the XCTest DerivedData path (PIDs 20577, 43716, 87719). Test-host service isolation remains a testing follow-up before repeatedly running host-based tests.
- Moved four superseded/failed test-app copies to the user's Trash under `SuperIsland-WE1-Debug-*-ZEfRhC.app`; no old app remains in the installation staging or package-backups directories. Production SuperIsland and WINS executable hashes are unchanged; no remote push occurred.

## 2026-08-30 signing checkpoint

- The user explicitly authorized adding the existing WE1 Debug certificate to current-user trust for the `codeSign` policy only.
- The existing certificate/private-key pair has SHA-1 `5BA2336137A6FF57316E24449C3914B56E3AC4C9`. The script reuses it rather than generating another identity.
- Corrected signing setup to use a temporary random PKCS#12 passphrase, self-signed `trustRoot` with the `codeSign` policy, and the default user domain (no `-d` admin-domain flag). Certificate constraints remain `CA:false`, digital-signature-only and code-signing-only; private-key import uses `-x -T /usr/bin/codesign`.
- The trust command is still awaiting system authorization; the latest read-only query reports `0 valid identities found`. Stable signing and installation have NOT completed, and the installed WE1 Debug app is unchanged.
- Re-ran 44 tests: 0 failures. Result: `/private/tmp/SuperIslandGoalBaselineDerivedData/Logs/Test/Test-SuperIslandWindowEnhancementTests-2026.08.30_21-06-06-+0800.xcresult`.
- A separate non-test Debug build succeeded at `/private/tmp/SuperIslandWE1SignedDerivedData/Build/Products/Debug/SuperIsland.app`; use this source for the final package so XCTest payloads are not included. Build concurrency was limited to two.
- No production app, WINS, TCC grant, or remote repository was modified this turn.

## Implementation and historical validation (not live acceptance)

- Froze the existing dirty-worktree baseline; Debug build and analyze succeeded before changes.
- Independently verified the installed WINS binary uses SkyLight/CGS, WindowServer descriptions, AX window IDs/actions, and WindowServer capture.
- Added a dynamically resolved, public-fallback-safe all-Space WindowServer inventory.
- Added fail-closed AX-to-WindowServer reconciliation and connected Dock and Cmd-Tab window lists.
- Removed unverified placeholder cards and added strict Renderer pixel fallback without changing action identity.
- Replaced the sibling-only Renderer assumption with verified descendant-process ancestry plus same-App-bundle executable containment; the rule matches the observed WeChat main → AppEx → Renderer hierarchy and remains fail-closed on ambiguity.
- Added WindowServer canonical identity validation to Mission Control hover and destructive close revalidation.
- Added a macOS unit-test target; 44 reconciliation, thumbnail-admission, path-boundary, Renderer-lineage, and guard-timer activity tests pass with zero failures or skips.
- Re-ran Xcode static analysis and Debug build successfully.
- Packaged, signature-verified, and installed WE1 Debug build `20260827141346` with source payload SHA-256 `533cddc093e4d09d2901b53f151cbc6f63ff70ffd69b66fe3901fd6327d7228f`.
- Removed the superseded WE1 Debug installation and package backup; production SuperIsland and WINS were not changed.
- Sampled the installed process: physical footprint stabilized near 55 MB and the stack sample showed WindowServer inventory paths sleeping rather than polling in the background.
- Removed the idle 1.25-second Dock guard wake-up: the timer now exists only while a verified Dock display-lock request is active and the Mac is awake.
- Added an opt-in stable signing path to the WE1 Debug packager and a local identity creator. The existing certificate/private key and current-user code-signing trust are valid; current installed build is `20260831224636` (see latest checkpoint).

## In progress

- Reviewed source is now installed under the user's separate authorization; keep further installation/replacement separate from code-only authorization.
- Perform real Dock, Cmd-Tab, WeChat, fullscreen, multi-Space and Mission Control acceptance; user-reported regressions are not considered accepted based on unit tests.
- Repeat resource/latency acceptance on the actual installed build with permissions. Test-host business startup isolation is implemented and tested, not an outstanding signing or TCC blocker.

## Guardrails

- No remote push or production release.
- Existing unrelated/uncommitted user changes are preserved.
- No unverified AX proxy may become an actionable window card.
- Private symbol failure degrades safely instead of blocking application launch.

## Completion audit

| Objective requirement | Current authoritative evidence | Status |
| --- | --- | --- |
| Independent WINS-style WindowServer/SkyLight-first route | Dynamic SkyLight/CGS resolver, public fallback, canonical PID/window-ID reconciliation, fresh destructive-action revalidation | Proved in current source and tests |
| Unified inventory and false-window rejection | Canonical reconciliation plus interior-pixel/AX-evidence rules and typed capture outcomes | Rules tested; user screenshots expose prior failures; new live acceptance pending |
| macOS 14–26 compatibility | Deployment target 14.0; private symbols are dynamically resolved and degrade to public APIs | Build-compatible; live 14/15 runtime not yet proved |
| All Spaces and multiple displays | Managed display Spaces and per-window Space memberships are queried; full-screen/off-Space identity remains addressable | Source present; live multi-Space/multi-display acceptance missing |
| Dock window preview | Canonical entries, strict Renderer ownership, shared first-click/hover handling | Source/model/pixel tests pass; installed behavior not yet verified |
| Cmd-Tab multi-window selection and close | Identity-checked pointer gestures, session guards, explicit AX-less preview-only state | Source/model tests pass; native switcher/WeChat/actual close acceptance missing |
| Mission Control Pro | Exact PID/window ID target, hover affordance, consumed click, forced fresh identity/frame validation before AX close | Source proved; permissioned desktop acceptance missing |
| Low latency and low resource use | Bounded inventory/capture caches, on-demand Dock guard timer; installed 20260831224636 with settings open and permissions effective sampled at 0.2% CPU, top MEM 64 MB over 15 seconds | Short baseline only; active interaction and long-run acceptance pending |
| Automated validation | 70 tests pass, zero failures/skips; isolated host, separate non-test Debug build and Analyze pass | Proved for reviewed source |
| WE1 Debug package/install | Fixed-certificate `20260831224636` installed and launched at exact WE1 Debug path, PID 20830; source/signature verified | Installation/startup verified; feature acceptance pending |
| Real desktop acceptance | Installed permissions were observed effective; latest user feedback still reports failures | Missing; not a permission gate |
| No release/push; preserve dirty worktree | Current branch remains dirty with pre-existing user edits; no remote mutation performed; production app and WINS untouched | Proved |
