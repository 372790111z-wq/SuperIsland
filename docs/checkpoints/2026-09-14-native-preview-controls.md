# WE1 原生预览交互检查点 · 2026-09-14

对应安装版本：20260914114254。保存来自 fix/we1-preview-controls-20260911 的完整当前实现，原基线 HEAD 为406f47c。范围包括继承的预览性能、梓栏共存、调度中心、窗口按钮、双微信展示与本轮原生 Cmd-Tab 提交修复；这是可回退的实现检查点，不代表所有功能完成验收。

## 验证与用户实测

- 当前125项Swift输入与最终260测试、独立Debug build/Analyze及候选包证据一致。
- 用户确认已测试卡片可以切换，系统Cmd-Tab图标条消失，重新进入后鼠标悬停正常。
- 安装包完整129项清单、固定证书签名和34份设置资源核验通过。
- ×和Command-W关闭、更多窗口状态/应用覆盖尚待后续实测。主微信取图问题按用户要求单独暂缓。

## 保存与恢复输入

本机完整存档目录：/Users/muyz/Projects/new super island/.codex-audit/checkpoints/we1-20260914114254-native-accepted/。完成清单记录真实commit和后续分支，包含完整实际安装app ZIP、源码快照、Git历史bundle、来源哈希、签名与验收记录。

Git收录明确核验的29个当前改动路径及本说明。应用包、构建产物、签名凭据、权限、私人数据和运行诊断原始文件不入Git。生成的Xcode工程被忽略，另外保存三份必要工程输入（含Package.resolved），锁定Aptabase0.3.11/cfd67fac2a228d448d9d2ac92ffc71589cc3ef00。Xcode工具链和依赖缓存不包含在源码提交中，不能把该提交单独称为逐字节可重建安装包。

恢复应用时先解压到新临时目录并核验签名，正常退出运行中的WE1后再替换。恢复源码应使用独立checkout，勿重置其他含未提交修改的worktree。原修复分支将保留检查点，后续关闭测试使用新分支；不推送远端。
