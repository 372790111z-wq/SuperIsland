# WE1：窗口分屏拖动与主岛悬停互斥修复

## 范围与来源

- 从 `5c33118` 创建 `fix/we1-snap-hover-20260918`，复用工作树 `we1-file-shortcuts`；旧功能分支仍指向原检查点。此前 185346 的原生测试证明，拖动窗口选择分屏区域时主灵动岛也会展开。
- 按已确认 WE1 PRD 的实现修正处理，沿用原需求和标注版本；没有新增布局、入口、设置或交互选择器。
- 只修主岛的悬停自动打开。保留主动打开、通知/HUD、已展开或锁定的主岛、Shelf 文件投放；Cmd-Tab、Dock、调度中心、Chrome 全屏移动和文件快捷键代码均未修改。

## 实现

1. 窗口监视器只在同一 PID、同一 AX 窗口的实际位置移动至少 4pt 后标记窗口拖动；普通鼠标按下、正文选择、文件拖放不会启动该状态，识别为窗口缩放时解除。主岛另在悬停入场与延迟执行时检查左键按住，覆盖顶部窗口被约束、尚未产生位置变化的拖动；这只暂停自动展开，不冒充实际窗口移动。
2. 独立 `WindowDragHoverGate` 管理拖动、指针位置和代次。主岛收到悬停、排入计时器和执行计时器时都检查；开始/结束/移出均使旧任务失效。
3. 结束时读取实际鼠标与可见 IslandPanel 范围，覆盖主岛入场回调迟到的情况。如果仍在主岛范围内，需要真正移出再进入；不重放拖动时的悬停，也不让浮层消失造成的迟到 exit 回调提前解锁。
4. stop、禁用、新序列、窗口失联、mouseUp、取消和 Aero Shake 共用幂等清理。只在实际移动期间运行 100ms 恢复检查，按钮连续抬起 300ms 后回收漏掉的 mouseUp；持续按住的长拖不超时。未增加 event tap 或消费原生事件。
5. 主岛视图在 compact 或错过 hover 回调时仍校正局部 hover 标记；隐藏面板或移除显示器不会永久阻止下一次悬停。真实指针停留与自动展开资格分开，主动点击/Shelf展示后仍取消自动收起；保留 Shelf 的命中、文件接收与完成回调。通知根据悬停晋升至完整展示的路径也服从此门控，其他 HUD 路径不变。

## 构建验证

- 16 项纯逻辑 XCTest 实际执行通过，0 失败：10 项悬停状态/旧任务/移出再进入/顶部约束，6 项丢失释放恢复/长拖/短暂按钮间隙。测试直接编译复制的生产门控源文件，SHA-256 与工作树一致，不读取 UserDefaults 或操作桌面。
- 完整 `xcodebuild build-for-testing` 和 `xcodebuild analyze` 通过；未启动 XCTest App 宿主。独立审查复核清理路径、计时器代次、局部 hover 校正和 Shelf 边界。
- 最终候选构建 `20260918003847`，稳定签名 `SuperIsland WE1 Debug Local Code Signing`，Bundle ID `com.workview.SuperIsland.WE1Debug`；签名及 designated requirement 校验通过。
- 候选源载荷指纹：`2d3d41939a6ad4883aa740fc6a88dc11dea78d9f73ac76888640a92d66643ab0`。
- 本轮构建/测试记录在 `build/SnapHoverRepair/`。候选实机结果单独记录，不能以以上检查代替安装与原生验收。

## 保留的首候选结果

`002729` 曾安装并通过4边缘、5个岛区域及提前取消的检查，但从已贴顶窗口开始、朝顶部继续拖动时，窗口尚未产生足够位移，主岛仍可能展开。该候选不算完整通过；`Native-002729/` 与 `first-candidate-*` 保留其日志和安装记录。补充的左键按住悬停保护用于覆盖这个实际边界。

## 003847 安装与实机结果

- 实际安装路径 `/Applications/SuperIsland-WE1-Debug.app`，构建 `20260918003847`，代码 `fa7bc5c`。安装记录核对了 101 个载荷条目、稳定签名、源载荷指纹和实际加载的调试动态库；安装后观察到的运行 PID 为 `61206`。首候选 `002729` 已被替换，其失败记录仍保留。
- 在唯一连接的 1512×982 屏幕、可用窗口区域 `[0,33,1512,886]` 上，使用 TextEdit 的专用 `WE1-分屏测试.rtf` 窗口测试；间距与 Stage Manager 预留均关闭。按用户授权使用 CGEvent 拖动，未把工具调用成功当作分屏成功。
- 9 个几何分屏检查通过：左、右、上、下 4 个边缘，以及岛内右半、下半、主区域右上、四分左上、四分右上 5 个区域。实际 AX 矩形与预期各分量误差不超过 2pt，其中左半与岛内右半高度比预期少 1pt。`plan.json` 还列有其他岛区域，这些规划项不计入本构建的测试结果。
- 3 个取消检查通过：进入顶部后提前松手、卡片间隙松手、从已选区域移出后松手。松手后窗口保持最后拖动位置，未提交分屏，分屏岛和目标框消失。此前失败的“贴顶窗口先向顶部拖动、再移到间隙”在本构建通过。
- 窗口右下角缩放到屏幕边缘、恢复测试尺寸两项通过：位置不变、尺寸确实改变，未误触发分屏。正文选择时窗口矩形不变，也未触发分屏；CUA 进一步观察到选中文字 `E1 分屏测试窗口`，两行测试正文保持原样。`move-neutral` 只为后续测试摆放窗口，不算验收项。
- 上述拖动、缩放、文字选择及释放后的采样中，主岛最大高度均为 32pt，所有结束记录的鼠标按钮均为释放状态。该结论来自移动中的采样、停留时约 25ms 一次采样及释放后的观察，并非无限时长保证。
- 正常悬停恢复另行实测：指针在外侧时主岛高 32pt，重新进入后展开到 257pt，离开后回到 32pt。此前两次尝试因前台是 Codex，在发送事件前被预检终止；最终辅助程序激活同一个专用 TextEdit 应用、复核身份后才发送指针移动并通过检查，未把预检终止算作产品失败或成功。

原始事件、测试辅助程序、`summary.json` 和输入文件 SHA-256 清单位于 `build/SnapHoverRepair/Native-003847/`；构建与安装证据位于 `build/SnapHoverRepair/`。这些是该安装版在所述范围内的自动化实机结果，不等同于用户验收。

## 保存、回滚与未覆盖项

- 代码保留在 `fix/we1-snap-hover-20260918` 的 `eb5f6b4`、`fa7bc5c`；旧功能分支和此前检查点保留，未推送远端。
- 修改前 `185346` 的完整应用备份在 `build/WE1-Debug/backups/Installed-SuperIsland-WE1-Debug-185346-before-snap-hover.app`，101 个清单条目及签名已核对。`002729` 的应用和失败证据也保留，不能把它作为本轮通过版本。
- 尚未覆盖实际 Shelf 文件投放、多显示器、其他应用和长时间运行。源码审查确认 Shelf/主动打开路径保留，但这不能代替对应实机验证。
- 下午新增的文件移到废纸篓、收起/展开全部窗口需要单独验证，本节结果不关闭那两项问题。整理本记录时，专用测试窗口仍保留以继续这些检查；最终场景恢复另行记录。
