# WE1 设置开放预览与包体积实验

- 日期：2026-09-20
- 源码基线：`a7a2750bce2793ba796c76e7a0065caa85d1274d`
- 分支：`feat/we1-local-settings-20260920`
- 阶段：交互需求稿待确认；未修改 Swift、PRD、项目配置或打包脚本，未安装或启动实验 App。

## 设置预览

`prototype/we1-local-settings-v0.1.html` 沿用深色设置侧栏、扩展列表和详情结构。拟先恢复高级页的显示器选择、诊断及版本信息，以及番茄钟启停、重载和四项既有设置。其他五个扩展、重置和更新分别显示尚未开放的原因。此范围待 HTML 确认后再同步正式需求，不构成代码实施或原生验收。

所有显示器、扩展与诊断状态都只在页面内模拟，刷新恢复默认。场景切换工具位于设置窗口外，无账号登录、真实系统操作或网络请求。

已在 Ego 独立任务空间验证：所有/指定显示器与断开回退；番茄钟启用、修改四项设置、重载保留设置、停用；活动空列表、启用失败后重试；五个待开放扩展均禁用且列表键盘焦点保留；扩展未发现后的重新查找；1280×900 与 900×700 无页面横向溢出。截图和只读结果保存在忽略目录 `build/LocalSettingsPreviewValidation/`。

## 包体积实测

测量采用十进制 MB，累加普通文件逻辑字节且不跟随符号链接；不代表清理软件的卸载统计口径。

| 产物 | 字节 | MB |
|---|---:|---:|
| 已安装 WE1 102559 | 46,148,131 | 46.15 |
| 当前源码 Release | 31,589,814 | 31.59 |
| Release 独立副本，剥离符号后 | 16,130,825 | 16.13 |
| 已安装原版，仅作背景比较 | 16,109,914 | 16.11 |

剥离符号副本较已安装 WE1 小 65.046%，未删减资源，剥离前后的主代码段哈希一致。源码与已安装版本存在最近的纯文案差异，原版源码与资源也不同，因此不把这些数值当作同一代码的严格构建模式对照。

成功构建启用 `-O`/whole-module，关闭测试性及 Debug dylib；实际 Swift 命令仍包含覆盖率插桩。副本仅用于体积测量，未签名、未启动、未做原生回归，不能当作已验收发行版。额外的全局 strip 构建因参数也作用到 SwiftPM 的 `Aptabase.o` 而失败，属于配置作用域问题。无覆盖率的正式构建配置尚待实施。

Xcode 未遵守尝试使用的禁止注册选项，首次构建及后续副本测量期间曾出现实验 App 的 Launch Services 登记。已仅按实验路径精确注销，并把测量副本移入 `.noindex` 目录、去除其 URL scheme。最终回读三个实验路径均无登记，两个安装版登记仍在；两个安装版的二进制哈希、体积和签名校验均保持正常。未重置系统注册数据库。

完整命令、配置、失败日志、注册纠正和字节/哈希证据在忽略目录 `build/WE1OptimizedSizeExperiment/` 的 `result.md`、`result.json` 和关联文件。测量副本位于其 `StrippedMeasurement.noindex/SuperIsland.app`，并非安装候选。

## 后续边界

先确认本轮 HTML，再按确认范围同步 PRD及取得代码实施授权。后续正式优化构建仍需明确覆盖率与包标识配置，签名和原生回归后方可安装。截图中的 193 MB 尚未核对清理软件的明细，不据此删除任何文件。
