# WE1：统一精简反馈文案

## 范围与基线

用户确认按场景精简提示，并要求提交到 GitHub。基线为 `33b7a12`，继续使用 `fix/we1-window-feedback-20260920`，保留此前检查点。原 263 项文案模板按 24 类场景审阅；本次只在既有调用点修改文字，未新增反馈框架或改变功能。

## 最终行为

- 普通成功保留“已调整”“已居中”“已移屏”；部分完成继续区分尺寸、位置和目标屏幕变化。
- 收起／展开反馈缩短，保留请求中、成功数量和未完成数量；恢复范围仍是原收起批次。
- 删除只确认命令已提交时显示“已请求移到废纸篓”。关闭请求成功显示“已请求关闭”，不据此宣称原生窗口已消失。
- 操作结果未知时提示检查窗口或文件状态，不提示立即重复删除或关闭。全屏控制失败不归因为应用主动拒绝。
- Dock 提示去掉“回读”“底边保护”“而不是假定成功”等内部措辞；到达目标屏幕与持续固定成功仍有区别。保留原目标设置不表示已恢复 Dock 位置。
- 权限类型、重新授权与重启要求继续区分；预览卡明确写“重启 WE1”，避免被误解为重启被预览的应用。
- 预览缺权限、无法匹配、没有窗口、正在生成和旧预览状态继续分别显示。

## 检查

- 10 个 Swift 文件，238 处字符串字面量修改。逐行对比基线，所有字符串引号外的代码均一致；未修改日志、识别键、AX 操作、时机、状态机或返回值。
- 独立复核覆盖请求／完成、未知结果、错误归因、Dock 固定与鼠标恢复边界，修正项复核通过。
- `git diff --check` 通过。
- macOS arm64 `build-for-testing` 通过；只构建测试目标，未运行会操作桌面的应用测试宿主。
- Xcode `analyze` 通过。构建环境仍有既有 CoreDevice／模拟器兼容提示和无关文件的 Combine 警告，不作为本次功能修改处理。
- 纯文案改动未新增镜像字符串测试。构建与核对产物留在忽略的 `build/FeedbackCopyImplementation/` 中。

## 保存与生效边界

本次只提交源码和本检查点，不把构建、截图或诊断日志加入本次提交；沿当前分支保存到用户现有的 GitHub 仓库，不合并主分支、不改作者仓库。当前分支此前的本地历史随分支推送保留。

未打包、安装或重启运行中的应用；代码完成不等于安装生效或实机验收。回退本次文案时使用对应提交的反向提交，或从基线 `33b7a12` 建立恢复分支，不覆盖现有工作。
