# WE1 最小化文档与全屏返回检查点 · 2026-09-14

对应安装版本：20260914145456，上一检查点为69cc592。本次保存在已核验完整实现上增加的最小化文档修复、6项回归测试及原生验收记录；后续使用独立本地分支测试多窗口混合状态，不推送远端。

## 修复与验证范围

TextEdit 的同一已保存文档最小化后由AXStandardWindow变成AXDialog，原规则因此丢弃真实AX对象，只留下没有操作对象和文档截图依据的历史卡片。现在仅允许明确最小化、非模态、非隐藏且有当前AXDocument依据的这类窗口进入后续核验。Dock和Cmd-Tab保持同一规则，仍要求真实进程与精确WindowID；最小化窗口不得模糊绑定，隐藏/模态/无文档对象和透明截图边界保持不变。

- 266项窗口增强测试通过，独立Debug build/Analyze通过；125项Swift输入与测试、构建、候选证据一致。
- 当前安装145456的129项清单、稳定签名和34份设置资源均已核验。
- 用户确认同一临时TextEdit A的最小化缩略图、点击恢复、系统Cmd-Tab图标条消失正常；同一PID/WindowID恢复、前台与AX焦点读回一致。
- 用户确认A全屏后跨桌面预览和点击返回正常。精确目标、所属Space、单张有图可操作卡、原生图标条消失及保持全屏均有对应证据；点击瞬间active-Space-ID未单独采样。
- 之前114254上的临时A/B关闭测试保留其独立验收范围。以上不能推广为所有应用、多窗混合状态、显示器或所有重新进入路径通过。主微信空白缩略图继续按用户要求暂缓。

## 保存与恢复

完整本机存档目录：/Users/muyz/Projects/new super island/.codex-audit/checkpoints/we1-20260914145456-minimized-fullscreen-accepted/。真实提交号、分支、文件清单与哈希由该目录checkpoint.json记录；保存实际安装app的完整ZIP、完整源码快照和tar、Git历史bundle及验收证据。

Git显式保存6项已审查修改和本说明。生成的Xcode工程、工作区元数据、Package.resolved在源码归档额外保存；依赖锁为Aptabase0.3.11/cfd67fac2a228d448d9d2ac92ffc71589cc3ef00。签名前构建来源hash与签后实际二进制hash分别保存。工具链、证书、依赖缓存和系统权限不纳入源码，因此不能声称仅凭Git可逐字节重建安装包。

恢复源码使用新checkout，避免覆盖其他worktree。恢复app先在新目录解压并验证清单/签名，正常退出WE1并保留当时安装后替换。原69cc592检查点、114254完整存档及安装前实际旧版均继续保留。
