# WE1：Finder 快捷删除的菜单范围修复

## 已确认问题

- 从 `b05fa6f` 创建 `fix/we1-trash-menu-20260918`，保留分屏修复分支及 `003847` 安装备份。用户已确认用 CGEvent 验证 ⌘D 收起/恢复与 ⌃D 删除专用测试文件。
- 003847 的桌面测试文件在真实 ⌃D 后仍留在原处，对照文件未改变。Finder 测试目录中，只读探针直接复制生产 executor 并把唯一 AXPress 替换为无操作，复现 `commandUnavailable`。
- 探针发现程序已找到 1 个正确命令，仍继续遍历无关子菜单；到深度 3 的 AXRadioGroup 时，队列 94 项加 8 项超过 96 上限，整次请求被拒绝。不是“没找到删除按钮”，也不是已执行但未确认。
- 本机 File 菜单结构只读回读为 1 个 AXMenuBarItem → 1 个 AXMenu → 41 个直接 AXMenuItem；唯一的正确 Trash 命令在第 2 层，启用状态为 true。

## 最小修复与验证

- 生产路径使用 `FinderTrashMenuScope`，仅读取 File 的唯一直接 AXMenu 及其直接命令，不请求 AXMenuItem 的子项。直接结构未知或不唯一时拒绝执行。
- 仍扫描全部直接命令，保留 96 项上限、24 个顶层菜单上限及标题+CmdDelete命令身份判断；重复命令仍拒绝。原 250ms 期限、两次命令身份比对、三次焦点及选区比对均保留。
- 没有增加输入事件模拟到应用，也未放宽删除范围；不改 PRD 和 20260917 标注，属于已确认功能的实现修正。
- 61 项隔离 XCTest 通过，0 失败，含 6 个新菜单范围测试。完整 build-for-testing 与 Analyze 通过；未运行会改变用户偏好的 App 测试宿主。
- 独立菜单测试最初因 CommandLineTools 缺 XCTest 无法运行；切到已有 Xcode 运行环境后 28 项通过。该结果与 61 项套件重合，不相加计数。
- 新测试覆盖大无关子树不被读取、嵌套 Trash 不授权、后置重复命令拒绝、异常结构、数量边界、读取/上下文失败传递。证据在 `build/TrashMenuRepair/`。

## 003847 收起/恢复实测

- 两轮真实快捷键完成，TextEdit A/B 恢复，预先最小化的 C 保持最小化；第二轮 18 个可比窗口前后最小化状态一致，其中 14 扇从未最小化变为最小化再恢复。
- 一扇 Chrome 全屏窗口未收起。精确只读结果：AXFullScreen=true，原生最小化按钮 AXEnabled=false；AXMinimized 虽可写但不等于本次操作成功。保留其全屏状态，没有自动退出全屏或改全屏移动代码。
- 前期 CUA 按键没有触发全局快捷键，不计产品通过或失败。CGEvent 辅助程序早期携带 Command 标志的 D 抬起留下按键状态；保护检查阻止再次发键，清理本次注入状态并改用无修饰抬起后，记录 D/修饰键均已释放。这是测试输入工具修正，未修改应用输入监视器。
- “已隐藏应用”的额外场景仅完成准备，后续按键因前台不符被拦截，不能算验收通过；最终场景清理另行记录。

新候选的安装与文件实际移入废纸篓结果在后续实测后补充，不能用上述单测或只读探针替代。
